# InkOS Studio 重构交接与待实现清单

更新时间：2026-06-26

这份文档用于换电脑后继续用 Codex 接力开发。当前项目主目录：

```text
/Users/nerosun/裂缝中的阳光/sxy/日常/code/my-turborepo
```

当前开发重点是 `apps/web` 里的创作平台页面，以及 `packages/inkos-adapter` 里的 InkOS Core 适配层。

## 当前上下文

项目正在把参考项目 InkOS Studio 的能力重构进当前平台，但模型配置已经从 InkOS 内部抽离成平台级公共模块。

参考项目本地路径：

```text
/Users/nerosun/裂缝中的阳光/sxy/日常/code/inkos
```

重点参考：

```text
packages/studio/src/pages/ServiceListPage.tsx
packages/studio/src/pages/ServiceDetailPage.tsx
packages/core
```

当前平台本地开发地址：

```text
http://localhost:3033
```

启动命令建议使用 Node 22：

```bash
source ~/.nvm/nvm.sh && nvm use 22 >/dev/null && pnpm --dir apps/web exec next dev --hostname 127.0.0.1 --port 3033
```

常用验证命令：

```bash
source ~/.nvm/nvm.sh && nvm use 22 >/dev/null && pnpm --filter web run check-types
source ~/.nvm/nvm.sh && nvm use 22 >/dev/null && pnpm --filter web run lint
source ~/.nvm/nvm.sh && nvm use 22 >/dev/null && node --experimental-strip-types --test apps/web/lib/novel-store.test.mjs apps/web/lib/provider-stream-parser.test.mjs
```

## 已完成能力

### 1. 平台级模型配置

已实现平台级“服务商管理 / 模型配置”页面，不再把模型配置放在 InkOS 小说模块内部。

已完成：

- 服务商列表分组：聚合 API、海外原厂、国产原厂、本地 / 订阅、CodingPlan、自定义服务。
- 服务商详情页：API Key 配置、显示/隐藏 Key、保存、删除配置。
- 测试连接后自动获取模型列表，移除了单独“获取可用模型”按钮。
- 支持 OpenAI-compatible、Anthropic Messages、Gemini、Ollama 等协议字段。
- 对齐 InkOS Studio 的服务商管理逻辑，修复过 DeepSeek、Moonshot、火山 Coding Plan 等 provider 差异。
- API Key 等模型配置存储在浏览器本地。

### 2. 主布局与菜单

已新增左侧平台菜单：

- 首页
- AI 小说创作
- 模型配置

AI 小说创作内部横向工具菜单已实现：

- AI创作
- 题材
- 文风
- 导入
- 市场雷达
- 环境诊断

页面滚动已调整为内容区域滚动，侧边栏在大宽度窗口下保持固定。

### 3. AI 小说创作基础工作台

已实现类 InkOS Studio 的对话式工作台结构：

- 左侧书籍列表。
- 一本书可以有多个会话。
- 中间为聊天 / Agent 交互区。
- 右侧为书籍上下文、章节、设定、预览。
- 没有书籍时显示创建第一本书籍的居中创建界面。
- 创建书籍时可选择模型。

### 4. IndexedDB 本地持久化

已实现书籍、会话、消息、章节、章节版本等本地持久化。

核心文件：

```text
apps/web/lib/novel-store.ts
apps/web/lib/novel-store.test.mjs
```

已支持：

- 书籍持久化。
- 会话持久化。
- 消息持久化。
- 章节持久化。
- 章节版本持久化。
- 章节状态、摘要、正文、审稿记录、版本记录持久化。

### 5. 书籍 / 会话 / 消息管理

已实现：

- 新建书籍。
- 书籍重命名。
- 书籍删除。
- 书籍归档。
- 书籍排序。
- 书籍搜索。
- 批量选择与批量操作基础。
- 新建会话。
- 会话重命名。
- 会话删除。
- 会话按书籍分组展示。
- 编辑上一条消息。
- 失败消息重试。
- 清空会话。
- 导出会话。
- 删除、归档后的 toast。
- prompt / confirm 已替换为项目内统一弹窗。

### 6. 普通聊天流式化与 Markdown 展示

普通 chat 已改为流式返回。

已补齐不同 provider 的 SSE / 流解析测试：

- OpenAI `delta.content`
- OpenAI `delta.reasoning_content` 兜底
- Anthropic `content_block_delta / text_delta`
- Ollama JSON line
- 多行 `data:` SSE
- 半包 SSE buffering

核心文件：

```text
apps/web/lib/provider-stream-parser.ts
apps/web/lib/provider-stream-parser.test.mjs
```

聊天消息、上下文预览、复审摘要等 Markdown 内容已优化显示。

### 7. InkOS Core Agent 接入

已把部分 InkOS Core 任务从静态 UI 接入真实 Agent 流程。

已接入并流式展示任务进度：

- 写下一章
- 审稿
- 修订本章
- 生成大纲 / 整理设定等部分动作
- 市场雷达
- 环境诊断

任务流式进度包括：

- 任务开始
- 正在读取书籍资产和上下文
- 正在调用对应 Agent
- 正在写回 IndexedDB
- 任务完成

已加入长任务稳定性基础：

- 当前任务状态栏。
- 任务进行中阻止重复任务。
- 取消任务。
- 错误提示。

### 8. 章节正文独立持久化

已将“写下一章”的正文从单纯聊天消息中拆出来，写入章节表。

章节字段包括：

- 章节编号
- 标题
- 正文
- 摘要
- 状态
- 字数
- 审稿记录
- 版本记录
- 更新时间

### 9. 审稿 / 修订 / 复审闭环

已实现：

- 审稿结果写回章节。
- 结构化解析审稿问题。
- 根据审稿意见修订本章。
- 修订后可再次审稿。
- 复审后标记历史问题为已解决 / 未解决。
- 复审 UI 显示本轮新增、历史问题、已解决、未解决。
- “审稿通过但仍有建议项”时，建议项展示为已解决，避免状态矛盾。
- 审稿 / 修订绑定到章节版本。
- 审稿问题结构化 v2：
  - 问题类型
  - 严重程度
  - 原文片段
  - 修改建议
  - 关联段落提示
  - 是否已修复
  - 关联修订版本标记
- 支持勾选审稿问题后“只修复选中的审稿问题”。
- 选中修订会把被修订的问题标记为已解决，并写回章节审稿记录。
- 修订成功后自动触发复审，并把复审结果写回章节。
- 审稿问题支持筛选：全部、未解决、已解决、本轮新增、历史问题。
- 审稿问题支持定位到章节正文段落，并打开章节编辑器辅助修改。
- 支持导出单章审稿报告 Markdown。
- 支持把审稿问题的段落级修改建议应用到章节编辑器草稿。

### 10. 章节计划自动联动

已实现：

- 章节生成后同步章节计划状态。
- 章节生成后自动提取章节摘要、角色状态变化、新增 / 回收伏笔、世界观增量。
- 提取出的章节资产增量会进入待确认队列，支持编辑、确认写入或忽略，确认后才沉淀到设定资产。
- 审稿状态变化同步到章节计划。
- 修订后同步章节计划。
- 书籍进度统计修复：已生成、待审稿、已定稿、总章节数。

### 11. 章节版本管理

已实现：

- 每次生成、编辑、审稿、修订、恢复都可产生章节版本记录。
- 版本记录展示。
- 从历史版本恢复。
- 版本 diff 纯函数。
- 版本与审稿 / 修订绑定。

最近新增：

- 章节正文编辑器。
- 编辑摘要和正文。
- 显示字数、段落数、摘要状态。
- Markdown / 正文预览。
- 保存后自动写入新版本。
- 本地自动保存章节草稿，重新打开同一章节时可恢复未保存内容。
- 查找 / 替换正文关键词，支持上一处、下一处、替换当前、全部替换。
- 全屏编辑模式。
- 版本对比 UI：旧版本 / 新版本左右栏。
- 新增行、移除行、未变行高亮。

### 12. 大纲、设定资产与写作上下文

已实现基础版：

- 书籍资产新增结构化章节计划 `outlineNodes`。
- 右侧工作台新增“大纲与章节计划”模块。
- 可新增章节计划节点。
- 可编辑章节标题、目标、冲突、角色、伏笔、目标字数。
- 可将结构化章节计划同步回项目 `chapters`。
- 书籍资产新增设定资产 `knowledgeAssets`。
- 右侧工作台新增“设定资产”模块。
- 支持世界观、角色、伏笔、地点、势力、物品、术语等资产类型。
- 可新增、编辑、删除设定资产。
- 书籍资产新增写作上下文选择 `contextSelection`。
- 右侧工作台新增“写作上下文”模块。
- 可选择写下一章时带入：
  - 大纲
  - 上一章摘要
  - 世界观
  - 角色
  - 伏笔
  - 审稿遗留问题
- 可设置目标字数、视角、节奏和本章高亮要求。
- `buildNovelWriteChapterInstruction` 已读取这些上下文选择，生成 WriterAgent prompt。
- 上下文预览已展示结构化章节计划、设定资产和写作上下文选择。

### 13. 任务日志持久化

已实现：

- IndexedDB 新增任务记录表。
- Agent 任务开始时创建任务日志。
- 任务执行进度写入任务日志。
- 成功、失败、取消都会落库最终状态。
- 工作台右侧展示最近任务日志。
- Workspace snapshot 按书籍分组返回任务列表，并按开始时间倒序排序。
- 已补充任务分组排序单测。
- 任务状态支持 queued / running / success / error / cancelled。
- 已支持创建持久化队列任务，并在执行时切换为 running。
- 已支持批量生成章节、批量审稿、批量修订。
- 批量任务按章节顺序逐个执行，中途失败会停止后续任务并标记未执行任务为 cancelled。
- queued / error / cancelled 任务可从任务日志继续执行或重试。
- 任务状态新增 paused / skipped。
- 批量队列支持暂停 / 继续、未开始项重排、单项跳过、完成报告导出。
- Agent 调用失败后会生成可恢复错误提示，区分授权、限流、超时、网络、取消和前置条件问题，并在任务日志展示恢复建议。

### 14. 队列、编辑器、审稿联动与导出发布增强

已实现：

- 章节编辑器新增段落导航。
- 章节编辑器显示目标字数进度。
- 支持多章节连续编辑：上一章 / 下一章快速切换。
- 版本对比支持搜索差异。
- 版本对比支持把旧版本移除段落恢复到当前编辑草稿。
- 审稿问题可映射到正文段落并作为高亮定位入口。
- 章节发布状态管理：未发布 / 待发布 / 已发布。
- 整本书导出 Markdown / TXT / docx。
- 按章节导出 Markdown。
- 按卷导出 Markdown。
- 导出创作日志 Markdown。
- 导出平台适配 TXT：起点格式、番茄格式。
- 批量任务完成报告可导出 Markdown。
- 已补充队列、段落导航、审稿高亮、导出 helper 单测。

## 当前重要文件

```text
apps/web/app/page.tsx
apps/web/app/page.module.css
apps/web/lib/model-settings.ts
apps/web/lib/model-settings.test.mjs
apps/web/lib/novel-store.ts
apps/web/lib/novel-store.test.mjs
apps/web/lib/provider-stream-parser.ts
apps/web/lib/provider-stream-parser.test.mjs
packages/inkos-adapter/src/index.ts
packages/inkos-adapter/src/server.ts
packages/inkos-adapter/package.json
```

## 待实现功能模块

### 1. 章节生产链路继续增强

优先级：高

目标：提高“写下一章”的质量和可控性。

待实现：

- 写下一章前增加上下文选择器。
- 可选择带入：
  - 章节计划
  - 上一章摘要
  - 世界观
  - 角色状态
  - 伏笔池
  - 审稿遗留问题
  - 读者爽点 / 悬疑点
- 生成参数：
  - 目标字数
  - 章节目标
  - 视角
  - 节奏
  - 爽点 / 悬疑点
  - 禁用词 / 风格约束
- 生成后资产提取增强：
  - 与伏笔池已有条目做更精确匹配
  - 读者爽点 / 悬疑点沉淀

建议下一步可先做：

```text
写下一章上下文选择器 v1
```

### 2. 审稿 / 修订系统产品化

优先级：高

当前已有审稿、修订、自动复审闭环，并完成结构化 v2、选中问题修订、筛选与正文定位。

待实现：

- 更精细的正文内原位高亮渲染。
- 审稿问题与版本 diff 的一键筛选 / 跳转。

建议下一步可做：

```text
审稿问题正文原位高亮 + diff 一键筛选
```

### 3. 大纲与章节计划模块

优先级：最高

原因：长篇创作质量最依赖大纲、卷纲、章节计划和上下文资产。

待实现：

- 独立的大纲编辑器。
- 卷 / 章层级管理。
- 拖拽调整章节顺序。
- 从大纲一键生成章节计划。
- 章节完成后反向同步大纲状态。
- 章节计划字段增强：
  - 章节目标
  - 冲突
  - 出场角色
  - 信息增量
  - 伏笔埋设 / 回收
  - 目标字数
  - 状态
- 大纲变化后提示是否重新同步章节计划。

建议下一步优先做：

```text
大纲与章节计划模块基础版
```

### 4. 设定资产管理

优先级：最高

原因：Agent 写作、审稿、修订都需要稳定的资产输入。

待实现：

- 世界观设定库。
- 角色卡。
- 角色关系图。
- 角色状态追踪。
- 伏笔池：
  - 已埋设
  - 推进中
  - 已回收
  - 遗忘风险
- 地点库。
- 势力库。
- 物品库。
- 术语表。
- 设定冲突检测。
- 资产变更记录。
- 写作时选择要带入哪些资产。

建议下一步和大纲模块一起做基础版：

```text
设定资产管理基础版
```

### 5. 题材 / 文风 / 导入 / 市场雷达 / 环境诊断深度接入

优先级：中高

当前这些工具已有 UI 和部分真实流程，但仍需要从“工具面板”升级为“资产生产入口”。

待实现：

- 题材分析结果真正写入项目资产。
- 文风样本管理。
- 文风分析结果写入风格约束。
- 文风迁移到章节生成 prompt。
- 导入已有章节后自动：
  - 拆章
  - 摘要
  - 提取设定
  - 提取角色
  - 提取伏笔
- 市场雷达接入真实榜单 / 卖点 / 风险输出。
- 市场雷达结果写入项目策略。
- 环境诊断检测：
  - 模型配置是否可用
  - 上下文资产是否缺失
  - 章节计划是否完整
  - 审稿遗留问题是否过多
  - 伏笔是否长期未回收

### 6. 编辑器体验继续升级

优先级：中

当前已经有章节正文编辑器和版本对比 UI。

待实现：

- 版本对比支持：
  - 跳转差异
  - 展示关联审稿问题
- 更细粒度的段落级恢复：替换当前位置，而不是追加到草稿末尾。

### 7. 任务系统

优先级：中高

当前已有长任务状态、取消、防重复、任务日志持久化、刷新恢复、失败任务重试、可恢复错误提示、任务队列和批量生成 / 审稿 / 修订。

待实现：

- 单个 Agent 调用的中途暂停 / 断点续跑。

### 8. 模型使用层

优先级：中

当前对话时可以选择模型，业务模型路由暂缓。

待实现：

- 最近使用模型。
- 默认写作模型。
- 不同任务临时选择模型：
  - 写作
  - 审稿
  - 修订
  - 大纲
  - 市场雷达
  - 环境诊断
- Token / 耗时 / 失败率统计。
- 模型调用日志。
- 后续小说生视频、图片生成接入后，再补业务模型路由。

### 9. 导出与发布

优先级：中

待实现：

- 更多平台格式适配与发布前校验。
- 发布记录和发布时间线。
- docx 样式模板增强。

### 10. 同步与备份

优先级：中

当前数据存在 IndexedDB，已支持本地 JSON 备份 / 恢复。备份会包含模型配置脱敏信息，但不会导出或恢复 API Key 明文。

待实现：

- 后续云端同步。
- 多设备冲突合并。

## 建议下一阶段执行顺序

建议下一个 Codex 会话优先执行：

```text
1. 审稿问题正文原位高亮 + diff 一键筛选
2. 更多平台发布格式校验和 docx 样式模板增强
3. 与伏笔池已有条目做更精确匹配
4. 单个 Agent 调用的中途暂停 / 断点续跑
```

原因：

- 当前“生成 / 审稿 / 修订 / 自动复审 / 版本 / 大纲 / 设定资产 / 写作上下文 / 章节资产待确认沉淀 / 选中问题修订 / 任务日志持久化 / 刷新恢复 / 任务重试 / 可恢复错误提示 / 队列 / 批量任务 / 编辑器自动保存 / 查找替换 / 全屏编辑 / 审稿报告导出 / 段落级建议应用 / 本地备份恢复”基础链路已经通了。
- 下一步可以优先补审稿 diff 联动、发布格式校验，以及伏笔池精确匹配。

## 给下一次 Codex 的建议提示词

可以直接复制下面这段给 Codex：

```text
请先阅读项目根目录的 INKOS_STUDIO_TODO.md，理解当前已完成能力和待实现清单。

接下来请优先实现：
1. 审稿问题正文原位高亮 + diff 一键筛选；
2. 更多平台发布格式校验和 docx 样式模板增强；
3. 章节资产增量与伏笔池已有条目的精确匹配；
4. 单个 Agent 调用的中途暂停 / 断点续跑。

要求：
- 先检查 apps/web/app/page.tsx、apps/web/lib/novel-store.ts、packages/inkos-adapter/src/index.ts 的现有结构；
- 不要重写现有链路；
- 沿用当前页面样式；
- 数据继续持久化到 IndexedDB；
- 增加必要的单元测试；
- 完成后运行 check-types、lint、novel-store/provider-stream-parser 测试；
- 如果 3033 已启动，请用 Chrome 做真实 UI 验证。
```
